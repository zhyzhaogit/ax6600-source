# Source-Layer Patches

Only place patches here when they must change the source tree itself.

Typical examples:

- target or device tree fixes
- NSS or driver changes that cannot be represented as package selection
- source-level build fixes required to keep AX6600 builds healthy

Keep patches grouped by subsystem and prefer small, reviewable series over large squashed drops.
